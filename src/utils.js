// utils.js

// 1. Функция для сложения чисел
export function sum(a, b) {
    return a + b;
  }

  // 2. Функция для проверки чётности
export function isEven(num) {
    return num % 2 === 0;
  }
  
  // 3. Дефолтный экспорт (строка)
 export default "Hello from utils!";